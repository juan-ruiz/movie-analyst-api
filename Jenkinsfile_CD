pipeline {

   environment { 

        registry = "squiroz/apitest" 
        registryCredential = 'squiroz'
        dockerImage = '' 

    }
  agent  {label "principal"}
    
  stages {
        
    stage('docker pull') { 
        steps { 
        sh "docker pull squiroz/apitest" 
            }
          }  
    stage('docker run') { 
        steps { 
        sh "docker run -d -p 3000:3000 squiroz/apitest:latest" 
            }
          }

    }
  }
